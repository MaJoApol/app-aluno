export const StatusBadge = ({ label }: { label: string }) => (
  <span className="text-xs font-semibold uppercase tracking-widest bg-gray-200 text-gray-500 px-2 py-1 rounded self-start">
    {label}
  </span>
);
